# Dashboard screenshots (portfolio)

Add PNG captures here for the root `README.md` gallery. Suggested shots:

| File | Content |
|------|---------|
| `control-panel.png` | Control Panel after a successful rank: results table + run metadata |
| `listing-detail.png` | Listing detail panel with diagnostics JSON tree expanded |
| `runs-compare.png` | Runs tab compare mode with summary tiles and diff table |
| `source-library.png` | Source Library filters + validation detail panel |

Keep images under ~200 KB each (compress if needed). Do not include real PII from production datasets—use fixture-driven local demos only.
